<h2>
<a href="/<%= get_attr("id", event) %>/">foto</a>

<% if num_of_sets > 1 %>
  <% for i in 0..(num_of_sets-1) %>
    <% if i == set_index %>
      <%= "<u><a href='/#{event_name}/photo/#{i}'>set #{i}</a></u>" %>
    <% else %>
      <%= "<a href='/#{event_name}/photo/#{i}'>set #{i}</a>" %>
    <% end %>
  <% end %>
<% end %>


<% if has_video(event) %>
<%= "• <a href='/#{event_name}/video'>video</a>" %>
<% end %>
</h2>